pagination:
  name: pagination
  in: query
  required: false
  description: Pagination for results.
  schema:
    type: object
    properties:
      limit:
        type: integer
        example: 10
        default: 10
      offset:
        type: integer
        example: 0
        offset: 0

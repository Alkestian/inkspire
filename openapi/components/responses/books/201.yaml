201:
  description: "Resource created successfully."
  headers:
    Location:
      $ref: "../../headers/Location.yaml"
  content:
    application/json:
      schema:
        $ref: "../../schemas/CreatedResponse.yaml"

201:
  description: "Resource created successfully."
  headers:
    Location:
      $ref: "../headers/Location"
  content:
    application/json:
      schema:
        $ref: "../schemas/CreatedResponse"

openapi: 3.1.0
info:
  title: Inkspire
  description: |
    This app allows for making lists of various types across different media and will eventually have
    fuzzy searching and rating functionality added to it.
  version: 0.0.1
paths:
  /books:
    get:
      summary: Get all books
      description: Get all books from the list
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                required:
                  - id
                  - title
                  - author
                properties:
                  id:
                    type: integer
                    minimum: 1
                  title:
                    type: string
                    minLength: 1
                    maxLength: 50
                  author:
                    type: string
                    minLength: 1
                    maxLength: 50
  /books/{bookId}:
    get:
      summary: Get a specific book
      description: Get a specific book by passing its bookId and return the book info
      parameters:
        - name: bookId
          in: path
          required: true
          schema:
            type: integer
            minimum: 1
      responses:
        "200":
          description: Success
          content:
            application/json:
              schema:
                type: object
                required:
                  - id
                  - title
                  - author
                properties:
                  id:
                    type: integer
                    minimum: 1
                  title:
                    type: string
                    minLength: 1
                    maxLength: 50
                  author:
                    type: string
                    minLength: 1
                    maxLength: 50
    post:
      summary: Add a book
      description: Add a specific book by bookId
      parameters:
        - name: bookId
          in: path
          required: true
          schema:
            type: integer
            minimum: 1

      responses:
    delete:

  /anime:
